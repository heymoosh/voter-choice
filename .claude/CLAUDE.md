# Claude Code Adapter

Root source of truth now lives in `AGENTS.md` and `CLAUDE.md`.

This file is retained as a Claude Code compatibility adapter for tools that read `.claude/CLAUDE.md` directly. Do not add new durable project rules here; update the root owners instead.
