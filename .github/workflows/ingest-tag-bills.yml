name: Tag Bills with Issues

on:
  # DISABLED 2026-06-28: the weekly schedule submitted ~29k untagged bills to the
  # Anthropic Batch API using the app's front-end API key (ANTHROPIC_VOTER_API),
  # draining the monthly budget (usage spikes observed 6/21 & 6/28, both Sundays).
  # Per owner directive, that key is for front-end user usage ONLY — no unattended
  # backend jobs.
  #   schedule:
  #     - cron: "0 9 * * 0"   # Sunday 09:00 UTC (was: after both ingest jobs complete)
  # DISARMED 2026-08-17: the manual trigger below used to run the same metered
  # Batch API path — never fully disarmed after 2026-06-28 despite the plan doc
  # calling it a live financial risk. It now just fails with instructions
  # (see the job below). Tagging runs on the Claude Max subscription instead —
  # scripts/ingest/_tag-bills.workflow.js, invoked manually in a Claude Code
  # session. Kept as workflow_dispatch (not deleted) so this file stays the
  # documented, discoverable place to look for "how does bill tagging run".
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Bitwarden Secrets Manager CLI
        # Pinned to bws-v2.0.0's published checksum:
        # https://github.com/bitwarden/sdk-sm/releases/download/bws-v2.0.0/bws-sha256-checksums-2.0.0.txt
        # On a deliberate version bump, update both the download URL and this hash together.
        run: |
          curl -sSLo bws.zip "https://github.com/bitwarden/sdk-sm/releases/download/bws-v2.0.0/bws-x86_64-unknown-linux-musl-2.0.0.zip"
          echo "ea6eb18f1270388f12d15a35f919ccb3488d32a777433baefe883d561f46fc73  bws.zip" | sha256sum -c -
          unzip -o bws.zip bws
          sudo install -m 755 bws /usr/local/bin/bws
          bws --version

      - name: Pull secrets from Bitwarden SM
        env:
          BWS_ACCESS_TOKEN: ${{ secrets.BW_ACCESS_TOKEN }}
        run: |
          # ---------------------------------------------------------------
          # DATABASE_URL — Neon Postgres connection string
          # ---------------------------------------------------------------
          DATABASE_URL=$(bws secret get 90abeeed-130e-4707-86ff-b446003770c2 --access-token "$BWS_ACCESS_TOKEN" --output json | jq -r '.value')
          echo "DATABASE_URL=$DATABASE_URL" >> "$GITHUB_ENV"
          echo "::add-mask::$DATABASE_URL"

          # ---------------------------------------------------------------
          # OPENSTATES_API_KEY — retained for context; not used by tag-bills
          # ---------------------------------------------------------------
          OPENSTATES_API_KEY=$(bws secret get 85bad136-3cbc-4062-9ccc-b4460037de19 --access-token "$BWS_ACCESS_TOKEN" --output json | jq -r '.value')
          echo "OPENSTATES_API_KEY=$OPENSTATES_API_KEY" >> "$GITHUB_ENV"
          echo "::add-mask::$OPENSTATES_API_KEY"

          # ---------------------------------------------------------------
          # ANTHROPIC_VOTER_API — REMOVED 2026-08-17. This job no longer calls
          # Claude at all (see the "Tagging is disarmed" step below), so it no
          # longer pulls the metered API key. Do not re-add this block without
          # re-reading docs/operations/ingest-and-tagging-plan-2026-07-31.md —
          # this exact secret pull is the "live financial risk" that plan
          # called out and asked to be removed first.
          # ---------------------------------------------------------------

          # ---------------------------------------------------------------
          # INGEST_FAILURE_WEBHOOK_URL — optional; webhook for ingest failure alerts.
          # UUID: <INGEST_FAILURE_WEBHOOK_BWS_SECRET_ID>
          # Replace the placeholder with the real BWS secret UUID once provisioned.
          # ---------------------------------------------------------------
          set +e
          WEBHOOK_SECRET=$(bws secret get <INGEST_FAILURE_WEBHOOK_BWS_SECRET_ID> --access-token "$BWS_ACCESS_TOKEN" --output json 2>/dev/null)
          WEBHOOK_STATUS=$?
          set -e
          if [ "$WEBHOOK_STATUS" -eq 0 ]; then
            INGEST_FAILURE_WEBHOOK_URL=$(echo "$WEBHOOK_SECRET" | jq -r '.value // empty')
            if [ -n "$INGEST_FAILURE_WEBHOOK_URL" ]; then
              echo "INGEST_FAILURE_WEBHOOK_URL=$INGEST_FAILURE_WEBHOOK_URL" >> "$GITHUB_ENV"
              echo "::add-mask::$INGEST_FAILURE_WEBHOOK_URL"
            fi
          fi

      - name: Tagging is disarmed here — run it via the Claude Max subscription instead
        run: |
          echo "::error::This workflow no longer runs bill tagging."
          echo "tag-bills-batch.ts called the metered Anthropic Batch API directly — the exact"
          echo "live financial risk docs/operations/ingest-and-tagging-plan-2026-07-31.md asked"
          echo "to be disarmed. Bulk LLM tagging now runs on the Claude Max subscription:"
          echo "  scripts/ingest/_tag-bills.workflow.js — run manually in a Claude Code session"
          echo "  (see that file's header for the full export -> workflow -> insert steps)."
          exit 1

      - name: Notify on ingest failure
        if: failure()
        env:
          WEBHOOK: ${{ env.INGEST_FAILURE_WEBHOOK_URL }}
        run: |
          if [ -z "$WEBHOOK" ]; then
            echo "INGEST_FAILURE_WEBHOOK_URL not set — skipping alert"
            exit 0
          fi
          curl -fsS -X POST "$WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "$(jq -n --arg job "${{ github.job }}" --arg run "${{ github.run_id }}" --arg repo "${{ github.repository }}" \
              '{text: "[voter-choice ingest failure] job=\($job) repo=\($repo) run_id=\($run)"}')"
