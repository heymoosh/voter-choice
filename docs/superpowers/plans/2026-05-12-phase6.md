# Phase 6 Implementation Plan

## Step 1: Types + Canonical Issues
- Add RankedIssues, ConfirmedConcerns types to src/lib/types.ts
- Create src/lib/canonicalIssues.ts with frozen 12-issue list + slugs

## Step 2: Issue Ranking Component
- Create src/components/IssueRanking.tsx using @dnd-kit/core + @dnd-kit/sortable
- KeyboardSensor + PointerSensor for accessibility
- data-testid: issue-ranking-list, issue-rank-item-<slug>, issue-rank-skip-button, issue-rank-confirm-button
- aria-live announcements on reorder

## Step 3: API Routes
- src/app/api/disambiguate-concerns/route.ts — POST, calls Claude, returns matchedIssues
- src/app/api/issue-counts/route.ts — GET by countyFips
- src/app/api/issue-counts/increment/route.ts — POST increment

## Step 4: Concern Disambiguation Component
- src/components/ConcernDisambiguation.tsx
- data-testid: concern-disambiguation-input, concern-disambiguation-submit, concern-mapping-confirmation, concern-mapping-issue-<slug>, concern-confirm-button

## Step 5: Polis Overlay Component
- src/components/PolisOverlay.tsx
- data-testid: issue-count-bar-<slug>, issue-count-value-<slug>, issue-count-county-label
- Graceful degradation if no counts

## Step 6: Integrate into Page + Update Prompts
- Update page.tsx to show IssueRanking + ConcernDisambiguation
- Update chatSystemPrompt.ts to include ranked issues + confirmed concerns
- Update generatePrompt.ts to include VOTER VALUES + CONCERN_INTERPRETATION blocks

## Step 7: Tests
- Unit tests for canonicalIssues, types, chatSystemPrompt, generatePrompt
- E2E tests for issue ranking + concern disambiguation flows
