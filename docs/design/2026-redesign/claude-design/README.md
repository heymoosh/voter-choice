# Claude Design front-end — drop zone

Unpack the Claude Design front-end code here (source only — no `node_modules/`, `.next/`, or build output). This is the **port-from reference** for the Phase-1 "Assess Congress" redesign.

**Integration rule:Treat this as front-end code. ** port the real backend logic INTO this Design code — never rewrite or reinterpret it. (Same pattern as the prior prototype: it lived in `../prototype/`, was ported into `src/prototype/VoterChoiceApp.tsx`, then removed.)

See the memory entries `design-pivot-alignment-first`, `design-prototype-integration`, and `project-launch-production-workflow` for the full contract.
