# Round-4 re-test checklist — Muxin's 2026-07-12 complaints vs what shipped

Companion to `docs/operations/round4-verbatim-feedback-2026-07-12.md` (your verbatim critique, recovered from the session transcript). Every fix below is MERGED TO MAIN and deployed to prod, so test directly on the live app. Complaints are numbered in the order you gave them; [Image #N] refers to your original screenshots.

| # | Your complaint (paraphrased; verbatim in companion doc) | What shipped | How to re-test |
|---|---|---|---|
| 1 | Mobile layout "looks pretty bad" — cards should stack when the screen gets smaller [Image #1] | PR #288: dg-grid 1/2/3-column breakpoints (was 1-col only below 560px) | Phone width (~390px): delegation overview cards stack single-column cleanly |
| 2 | Can't review the seat of the not-up-for-election member from this screen [Image #1] | PR #286: not-up seats fully reviewable but never decidable — verdict block replaced by an info band; overview row gets an entry; rail shows "Record only · next up YYYY" | Open the senator who isn't up in 2026: full record/funding visible, NO keep/replace buttons, rail labels it "Record only" |
| 3 | Widescreen: can't scroll down to even see the third member; no scrollbar | PR #288: root cause was `.ws-shell` clipping to 100vh with overflow hidden; un-clipped, document flow restored | Widescreen Safari: page scrolls to the third (Senate) card normally |
| 4 | Huge dead gap at the bottom; right rail hard to use (cursor has to sit over white space) [Image #2] | PR #288 (same scroll-model fix) + sticky rail | Same page: no dead gap, rail usable while scrolling |
| 5 | Mobile: rail completely disappeared + two scrollbars [Image #3, #4] | PR #288: mobile list-first contract, sticky overlay header (back button no longer scrolls away) | Phone width: rail content reachable, single scrollbar |
| 6 | Data bug: only 2 of 3 issues shown as rows, then the modal shows a different set (AI appears, groceries vanish); all 3 should show even with no data [Image #5, #6] | PR #284: `issuesForSeatCard()` — every user issue renders its row, per-issue (not aggregate) fractions, honest "no available information" rows | Pick 3 issues incl. one thin one: seat card shows all 3 rows, fractions differ per issue, modal matches the rows. **KNOWN GAP: the head-to-head duel view still has the old bug** (fast-follow card is To Do, not yet built) |
| 7 | Funding preview doesn't explain the bars (orange vs red) — legend buried in expanded view [Image #7, #8] | PR #285: compact legend in the collapsed funding glance | Collapsed card: bar colors labeled without expanding |
| 8 | "Full voting record" / "Funders & influence" buttons impossible to find; expected to click Voted With You [Image #9, #10] | PR #285: "see all" wired INTO the Voted-With-You align band; money line itself is now the clickable disclosure | Click directly on the align band / money line — both should open the deeper views |
| 9 | Text sizes / hierarchy cluttered, overlapping, "ugly UI" [Image #11–#13] | PR #285 typography + align-band canvas port; Round-3.1 polish confirmed subsumed into main | General look-over of the rep card at both widths |
| 10 | Per-issue drilldown should look like the Keystone "Select a vote" design with expand carets [Image #14] | PR #285: canvas VoteCard drilldown layout + caret direction fix | Click into an issue: Keystone-style roll-call list, carets indicate expandable |
| 11 | Keystone FunderBars is better than our funding mix; keep our named-issue-PAC pills but smaller [Image #16, #17] | PR #285: FunderPanel canvas port + compact named-PAC pills | Expand funding: FunderBars-style breakdown, PAC pills compact |
| 12 | Showing all challengers here is confusing and premature — "are they still in the race?" [Image #18] | Two parts: PR #285 removed challenger bars from the incumbent card (MoneyGapScale = subject vs median only); the "are they still in the race" data problem became the ENTIRE roster epic — real fix arrives when `OFFICIAL_ROSTER_ENABLED` flips | Incumbent card: no challenger clutter. Challenger accuracy: re-test AFTER the flag flip |
| 13 | "25.1× median" means nothing — median of what, and why show it [Image #19] | PR #285: reworded to "≈N× the typical <office> campaign" | Funding glance: the multiplier reads with context |
| 14 | Keep/Replace buttons on a candidate who isn't even up — remove [Image #20] | PR #286 (same as #2) | Not-up seat: no verdict buttons anywhere |
| 15 | H2H: can't tell if Cornyn vs Crockett is even the same seat — accuracy doubt [Image #20] | PR #287: explicit seat line, "2026 FEC filer for this seat" provenance, pinned incumbent chip; challenger seat-scoping verified (state+year+office filtered) | H2H view: seat statement at top, both candidates labeled to the same seat |
| 16 | "What happened to him? Is he not listed anymore? Did we pull the right information?" [Image #21] | Roster epic (all 50 states built + verified against official sources) — visible after the flag flip | Re-test after flip |
| 17 | H2H should show the full funding comparison (how much more raised, from whom) | PR #287: funding comparison section + whole-field money scale moved here ("Everyone running for this seat") | H2H view: funding compare section present |
| 18 | Not sure mobile-responsive works everywhere; not sure everything is accessible, font sizes/contrast OK | Lane G audit ran; output mostly lost with the session scratchpad. 8/12 findings recovered onto the board card "[P1] Round-4 responsive/a11y audit (Lane G)…" — NOT yet fixed | Not fixed yet — the recovered list (intake Send overflow at 390px, PolisStand kicker, hp-sheet bleed, Terms page, touch targets, print caption size, tag contrast) is queued |

## Not fixed yet (don't expect these to pass)
1. H2H duel per-issue rows (complaint #6's known gap) — To Do card exists.
2. The Lane-G responsive/a11y items (complaint #18) — card exists with the recovered findings.
3. Challenger staleness/accuracy (#12, #16) — correct data exists behind `OFFICIAL_ROSTER_ENABLED`; visible to you (and users) only after the flip card fires.
