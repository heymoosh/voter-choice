# Run Log

## Next

Begin Phase 0.0 — Commit all existing planning docs to git (docs/, CLAUDE.md, .gitignore, .claude/) with message `phase0.0: add experiment design docs and ballot prompt`. Then proceed to Phase 0.1 — Write the Feature Spec (`docs/PROJECT_SPEC.md`). Read `docs/BALLOT_PROMPT.md` to derive the data model and JSON schema. Refer to the Phase 0.1 section of `docs/EXPERIMENT_DESIGN.md` for requirements. Note: the spec must include required `data-testid` attributes for key interactive elements (needed by the shared Playwright e2e tests in Phase 0.3b).
