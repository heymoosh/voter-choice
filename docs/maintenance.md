# Maintenance profile — voter-choice

Generated by the `maintenance-audit` skill against the shared rubric at
`/Users/Muxin/Documents/GitHub/simple-kanban/docs/maintenance-loops.md`. This is the
**first** audit run for this repo (no prior `docs/maintenance.md` existed) — the record
below is the diff base for the next run.

**Date:** 2026-07-20

## Profile

- **Exposure class: C** — user data / auth / trust. Evidence: public production deploy
  (`.github/workflows/deploy.yml`, live Vercel project per `.vercel/`), a Postgres-backed
  app (Neon via `@neondatabase/serverless`, `drizzle-orm`) with real user-submitted PII
  (`db/migrations/0017_add_roster_feedback.sql` — free-text + address/district context;
  donor/member-stats ingestion tables), and a public GitHub repo (`private: false`). No
  traditional login/session system was found (no `middleware.ts`, no `*auth*`/`*session*`
  source files), but per the rubric's tie-break rule ("when torn between two classes, pick
  the higher one") the PII intake + public deployment alone puts this at C, not B.
- **Maturity stage: 0 (per rubric default)** — `docs/maturity.md` does not exist in this
  repo, and the rubric says to default to Stage 0 when it's absent. Flagging this as a
  likely **mismatch worth fixing**, not acting on it: the repo's actual CI posture looks
  well past Stage 0 (branch protection requires `test`, `e2e`, `mutation`;
  GitHub secret scanning + push protection are already ON at the repo-settings level; a
  live mutation-testing gate exists). Recommend a follow-up to write `docs/maturity.md` so
  future audits (and the maturity ladder that gates other tooling) reflect reality instead
  of the rubric's absent-file default. Not filed as one of the 5 backlog cards below
  (deliberately over the noise cap on a first-run audit) — worth adding at the next
  grooming pass if this file is still missing.
- **Ecosystems:** npm only (Node 22, `package.json`/`package-lock.json`; Next.js 15 /
  TypeScript / Drizzle / Vitest / Playwright / Stryker). No Python, Ruby, Go, or Rust
  manifests found. → npm-ecosystem scanners only (Dependabot `npm` + `github-actions`,
  `npm audit`).
- **Change velocity: very high.** 363 commits in the last 30 days across 22 distinct
  days. → weekly/biweekly calendar loops are well justified (no demotion to monthly or
  on-demand for a dormant repo).
- **What already exists (confirmed via `gh api`, checked 2026-07-20):**
  - Secret scanning: **enabled**.
  - Secret scanning push protection: **enabled**.
  - Dependabot version-update config (`.github/dependabot.yml`): **absent**.
  - Dependabot security updates / vulnerability alerts (repo setting): **disabled**
    (`GET /repos/heymoosh/voter-choice/vulnerability-alerts` → 404).
  - Dependency-audit CI job (blocking or advisory): **absent** — the only dependency
    finding on the board (`[P1] Bump Next.js 15.5.12 -> 15.5.20+`, now Done) was a
    one-off manual `idea-scout` catch, not a recurring scanner.
  - Event-driven security-review gate on sensitive PRs: **absent**.
  - Codebase-health-report loop, docs-drift loop, CI-failure→card bug-triage loop,
    scheduled idea-scout cadence: no `.github/workflows/schedule:` or board evidence
    found for any of them — likely run (if at all) as ad hoc sessions, not standing
    loops with a trigger.

## Derived plan vs. rubric loop table (Class C row)

| Loop | Rubric (Class C) | Status here | Action |
|---|---|---|---|
| Dependency scan | weekly; block merge on high severity | absent | **PR**: add `.github/dependabot.yml` + `dependency-audit.yml` (see below; not yet wired into required checks — see PR body) |
| Secret scanning | on + push protection | already on (repo setting) | skip — already covered |
| Deep security review (agent) | event, mandatory pre-merge on sensitive PRs + quarterly deep pass | absent | **PR**: deterministic path-detection half (`security-review-required.yml`); **card**: the agentic review-loop + quarterly deep pass itself (NEEDS DECISION — changes merge friction) |
| Codebase health report → refactor cards | monthly | absent as a loop (one-off `refactor-proposal-2026-07.md` exists) | **card** |
| Bug triage | event-only CI failure/error report → card + error-tracker feed | no error-tracker (no Sentry/equivalent in `package.json`) | **card** (NEEDS DECISION — new vendor/cost) |
| PR/branch/worktree cleanup | eod-cleanup + weekly stale sweep | eod-cleanup exists per prior orchestrator record | skip — already covered |
| Idea generation from epics (propose-cards) | weekly / each pre-flight | evidenced in use (idea-scout-origin cards on the board) | skip — already covered |
| Idea scout (net-new) | biweekly | used ad hoc (2 cards dated 2026-07-14), no standing cadence found | **card** — confirm/wire the biweekly cadence |
| Docs drift | weekly scan of merged PRs → cards | absent | **card** |

That produced 6 candidate agent-loop cards against the ≤5 cap; the quarterly-deep-pass
half of the security-review row was folded into the same card as the event-driven half
(one card, two cadences of the same loop) to stay at 5. See the PR and the 5 filed cards
for exact scope.

## What shipped from this audit

1. **`docs/maintenance.md`** (this file).
2. **Draft PR** on branch `maintenance-audit/voter-choice-2026-07-20` — held for Muxin's
   security review per the driving card's `DECISION: stage` (not auto-merged, not
   auto-merge-armed):
   - `.github/dependabot.yml` — npm + github-actions ecosystems, weekly.
   - `.github/workflows/dependency-audit.yml` — `npm audit --omit=dev --audit-level=high`
     on PR/push/weekly schedule. **Not wired into required status checks** (branch
     protection edits are admin-gated, left to Muxin per standing convention), but the
     baseline **is green as of 2026-07-20** — the high-severity findings this job surfaced
     at introduction were fixed in this same PR rather than allowlisted:
     - `form-data` CRLF injection (GHSA-hmw2-7cc7-3qxx), transitive via
       `@anthropic-ai/sdk` → `@types/node-fetch` — cleared by `npm audit fix`,
       lockfile-only, no direct dependency changed.
     - `playwright` installing browsers without verifying the SSL certificate
       (GHSA-7mvr-c777-76hp) — cleared by moving `@playwright/test` 1.52.0 → 1.61.1,
       pinned exactly to match every other devDependency in this repo.

     Two **moderate** findings remain and are deliberately not fixed: `postcss` <8.5.10
     (GHSA-qx2v-qp2m-jg93) is reachable only through `next`'s bundled copy, and npm's only
     offered remedy is a downgrade to `next@9.3.3` — a multi-major breaking change to clear
     a moderate. They sit below this gate's `high` threshold and clear when `next` ships a
     bumped `postcss`. So there is no allowlist and no accepted-risk decision to make: the
     gate is honestly green, and is safe to make a required check.
   - `.github/workflows/security-review-required.yml` — the deterministic half of the
     "mandatory pre-merge on sensitive PRs" rule: detects PRs touching
     `src/app/api/**`, `db/**`, `.env*`, or anything matching `*secret*`/`*auth*`/
     `*token*`/`*session*`, and fails until a `security-reviewed` label is applied. The
     judgment half (who reviews, on what) is intentionally left to Muxin + the Backlog
     card below, not automated.
3. **5 Backlog cards** appended to `docs/operations/voter-choice-backlog.md`
   (`ORIGIN: maintenance-audit 2026-07-20`), for the agent-loop gaps above.

## Re-run trigger

Next audit: on a `docs/maturity.md` addition (a real maturity-stage transition), 2026-10
(quarterly), or on demand.
